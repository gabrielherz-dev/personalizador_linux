# ~/.dialogrc
#
# ~/.dialogrc - Configuración completamente oscura

use_shadow = OFF

screen_color = (BLACK,BLACK,ON)
dialog_color = (WHITE,BLACK,ON)
title_color = (CYAN,BLACK,ON)
border_color = (WHITE,BLACK,OFF)
button_active_color = (YELLOW,BLACK,ON)
button_inactive_color = (WHITE,BLACK,ON)
button_label_active_color = (BLACK,WHITE,ON)
button_label_inactive_color = (WHITE,BLACK,ON)
# Change form background color
#form_text_color = (WHITE,CYAN,ON)
#
            # Input box color
            inputbox_color = dialog_color

            # Input box border color
            inputbox_border_color = dialog_color

            # Search box color
            searchbox_color = dialog_color

            # Search box title color
            searchbox_title_color = title_color

            # Search box border color
            searchbox_border_color = border_color

            # File position indicator color
            position_indicator_color = title_color

            # Menu box color
            menubox_color = dialog_color

            # Menu box border color
            menubox_border_color = border_color

            # Item color
            item_color = dialog_color

            # Selected item color
            #item_selected_color = button_active_color
            item_selected_color = button_label_active_color

            # Tag color
            tag_color = title_color

            # Selected tag color
            tag_selected_color = button_label_active_color

            # Tag key color
            tag_key_color = button_key_inactive_color

            # Selected tag key color
            tag_key_selected_color = (RED,BLUE,ON)

            # Check box color
            check_color = dialog_color

            # Selected check box color
            #check_selected_color = button_active_color
	    check_selected_color = button_label_active_color

            # Up arrow color
            uarrow_color = (CYAN,WHITE,ON)

            # Down arrow color
            darrow_color = uarrow_color

            # Item help-text color
            itemhelp_color = (WHITE,BLACK,OFF)

            # Active form text color
            #form_active_text_color = button_active_color
            form_active_text_color = button_label_active_color
            # Form text color
            #form_text_color = (WHITE,CYAN,ON)
	    form_text_color = (BLACK,WHITE,ON)
            form_text_color = (CYAN,BLACK,ON)

            # Readonly form item color
            #form_item_readonly_color = (WHITE,BLACK,ON)
	    form_item_readonly_color = (YELLOW,BLACK,ON)

            # Dialog box gauge color
            gauge_color = title_color

            # Dialog box border2 color
            border2_color = dialog_color

            # Input box border2 color
            inputbox_border2_color = dialog_color

            # Search box border2 color
            searchbox_border2_color = dialog_color

            # Menu box border2 color
            menubox_border2_color = dialog_color

